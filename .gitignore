# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Node / frontend
node_modules/
frontend/dist/
*.local

# Env / secrets
.env
# Plaintext sealed-secret input — never commit (only sealed-secret.yaml is safe)
secret.plain.yaml

# Data / media
data/
*.mp4
*.wav
*.mkv

# Models cache
.cache/
models/
